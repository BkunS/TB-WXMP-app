<!--pages/orders/orders.wxml-->
<view class="fullWidth">
  <view class="header">
    <view class="fullWidth" style="height: {{statusBarHeight}}rpx; background-color: white"></view>
    <view class="header-topNavbar" style="height:{{topNavHeight}}rpx;">
      <view wx:if="{{canNavBack}}" class="header-topNavbar-navWrapper alignStart">
        <navigator open-type="navigateBack" class="header-topNavbar-nav" style="height: 100%">
          <image src="../../assets/back.png" mode="aspectFit" id="back" class=".header-topNavbar-nav-image" bindload="iconLoad" style="width: {{backWidth}}rpx;" />
        </navigator>
      </view>
      <view wx:else class="header-topNavbar-navWrapper alignStart">
        <navigator open-type="navigateBack" class="header-topNavbar-nav" style="height: 100%">
          <image src="../../assets/backEmpty.png" mode="aspectFit" id="back" class=".header-topNavbar-nav-image" bindload="iconLoad" style="width: {{backWidth}}rpx;" />
        </navigator>
      </view>
      <view class="header-topNavbar-navWrapper" style="flex-direction: column;">
        {{pageName}}
      </view>
      <view class="header-topNavbar-navWrapper alignEnd" style="padding: 0 20rpx">
        <image src="../../assets/backEmpty.png" mode="aspectFit" id="back" class=".header-topNavbar-nav-image" bindload="iconLoad" style="width: {{backWidth}}rpx;" />
      </view>
    </view>
  </view>
  <view class="placeholder" style="height: {{placeholderHeight}}rpx;">
  </view>
  <view wx:if="{{pageContents.globalMsg}}">
    <image src="{{pageContents.globalMsg}}" mode="widthFix" class="fullWidth" style="margin-bottom: -10rpx" />
  </view>
  
  <view class="orders-list-wrapper">
    <view wx:for="{{orders}}" wx:key="id" class="orders-list">
        <view class="order-info">
            <!--<image src="{{item.image}}" class="item-image" mode="widthFix"></image>-->
            <view>
              订单编号：{{item.id}} 
            </view>
            <view>
              下单时间：{{item.timeStr}} 
            </view>
            <view>
              收件人：{{item.shipment.name}}
            </view>
            <view wx:if="{{item.shipment.tel}}">
              联系方式：{{item.shipment.tel}}
            </view>
            <view> 
              收货地址：{{item.shipment.address}}
            </view>
        </view>
        <view class="order-product-list">
          <view wx:for="{{item.products}}" wx:key="productId" class="product-item">
            <image src="{{item.image}}" mode="widthFix" style="width: 28%; min-width: 28%;" />
            <view class="product-item-detail"> 
              <view class="product-item-detail-title">{{item.displayName}} </view>
              <view class="product-item-detail-content"> 款号：{{item.productInfo.styleId}} </view>
              <view class="product-item-detail-content"> 颜色：{{item.productInfo.colorName}} </view>
              <view class="product-item-detail-content" wx:if="{{item.productInfo.size !== 'N/A'}}"> 尺寸：{{item.productInfo.size}} </view>
              <view class="product-item-detail-content"> 数量：{{item.quantity}} </view>
            </view>
            <view class="product-item-price">
              <view class="product-item-detail-price">￥{{item.salePrice * item.quantity}} </view>
            </view>
          </view>
        </view>
        <view class="order-totalPrice"> 
          <view> 合计：{{item.finalPriceStr}} </view>
          <view> <button bindtap="" size="mini" hover-class="button-hover" 
          style="margin-left: 0; margin-right: 0; color: white; background-color: #0c2340"> 查看物流 </button> </view>
        </view>
    </view>
  </view>
</view>
